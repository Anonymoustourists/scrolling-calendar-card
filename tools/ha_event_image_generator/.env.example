# Copy to .env (same folder) and fill values.
# Do NOT commit .env (it is ignored by .gitignore).

# OpenAI API key used by the generator
OPENAI_API_KEY=

# Home Assistant base URL from where THIS script runs.
# If running on your MacBook Air, use your HA host on LAN (e.g. http://m4mm:8123 or http://<LAN_IP>:8123)
HA_BASE_URL=http://m4mm:8123

# Home Assistant Long-Lived Access Token
HA_TOKEN=

# Optional overrides for the deploy script (scripts/push_to_m4mm.py)
M4MM_HOST=m4mm
# Root "www" path on the m4mm host
M4MM_WWW_ROOT=/Volumes/ScriptsM4/newhome/ha-config/www
